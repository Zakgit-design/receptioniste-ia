import { PlaceholderPanel } from "@/components/placeholder-panel";

// Écran Appels du Dashboard Client — placeholder, voir docs/roadmap.md, tâche #67.
export default function AppelsClientPage() {
  return (
    <PlaceholderPanel description="Liste des appels reçus par votre entreprise, avec fiche de détail. Arrive dans une prochaine tâche." />
  );
}
